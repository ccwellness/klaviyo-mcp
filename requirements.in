# requirements.in — top-level intent only; pip-compile derives the rest.
# Managed with pip-tools (CS-018). To regenerate the lock file:
#   pip-compile --generate-hashes --output-file=requirements.txt requirements.in
# To install: pip install --require-hashes -r requirements.txt

# --- Core: MCP transport ---
mcp==1.2.0

# --- Klaviyo REST client (direct httpx; no official SDK) ---
httpx==0.27.2

# --- REST transport ---
flask==3.0.3
# Gunicorn: WSGI server for the containerised REST API (Linux/container deploys only).
# On Windows dev, use: flask --app api run.
gunicorn==22.0.0

# --- Config ---
python-dotenv==1.0.1              # .env loading (sibling convention)

# --- Observability (CS-019) ---
structlog==24.4.0                 # structured logging

# tomllib is stdlib on Python 3.11+ — no TOML dependency required.

# --- Dev/test (CS-004, CS-018, CS-020) ---
pytest==8.3.3
pytest-cov==5.0.0
ruff==0.6.9
mypy==1.11.2
pip-tools==7.4.1
pre-commit==3.8.0
